id: manage-provider-public-ip-pools
title: Manage Provider Public IP Pools Statechart
source_flow: ../ui-flows/manage-provider-public-ip-pools.yaml
rbac_ref: ../rbac.yaml
initial: pool_list_view

states:
  pool_list_view:
    permissions: [manage_provider_public_ip_pools]
    on:
      CLICK_POOL: { target: pool_detail_page }
  pool_detail_page:
    permissions: [manage_provider_public_ip_pools]
    on:
      CLICK_BACK: { target: pool_list_view }
ai-hints:
  summary: Read-only pool list + detail; IP allocation within pool detail.
