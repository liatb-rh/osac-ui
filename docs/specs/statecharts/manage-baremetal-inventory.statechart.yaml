id: manage-baremetal-inventory
title: Manage Baremetal Inventory Statechart
source_flow: ../ui-flows/manage-baremetal-inventory.yaml
rbac_ref: ../rbac.yaml
initial: bmi_list_view

states:
  bmi_list_view:
    permissions: [manage_baremetal_inventory]
    on:
      CLICK_HOST: { target: bmi_detail_page }
  bmi_detail_page:
    permissions: [manage_baremetal_inventory]
    on:
      CLICK_BACK: { target: bmi_list_view }
ai-hints:
  summary: Provider baremetal host inventory — read-only list + detail.
