# ui-flow: manage-network-classes
# Generated: 2026-06-18

flow: manage-network-classes
title: Manage Network Classes (Provider)
summary: Read-only network class catalog (roadmap preview).
statechart_ref: ../statecharts/manage-network-classes.statechart.yaml
rbac_ref: ../rbac.yaml

rbac:
  required_permissions_by_step:
    nc_list_view: { view: manage_network_classes }

steps:
  - id: nc_list_view
    title: Network Classes list
    goal: Browse available network classes and their capabilities.
    route: /provider/network-classes
    ui:
      components:
        - PageLayout (title="Network Classes")
        - Table (Name, Title, Description, Capabilities, Default, State)
        - Label (state, default flag)
    states:
      loading: { description: Spinner. }
      populated: { description: Table rendered. }
    interactions:
      - "Read-only — no create/edit in current implementation."
    validation_rules: []

implementation_status:
  as_of: "2026-06-18"
  code_refs:
    - apps/app-frontend/src/pages/infrastructure/NetworkClassesPage.tsx
  deployment_variants: [standalone]
  deferred:
    - Create / edit network classes
