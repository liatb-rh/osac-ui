# OSAC_WORKAROUND_REMOVE(vite-dev-bearer): optional dev-only bearer when sessionStorage has no token.
# VITE_DEV_BEARER_TOKEN=

# Enable MSW (Mock Service Worker) standalone mode — no backend required.
# Use `pnpm dev:standalone` from the workspace root, or set this manually.
# VITE_MSW=true
